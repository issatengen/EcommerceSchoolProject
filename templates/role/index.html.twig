{% extends 'base.html.twig' %}

{% block title %}Role index{% endblock %}

{% block body %}
{{ include('partials/admin_navbar.html.twig') }}
<div class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="mb-0">Roles</h1>
        <a href="{{ path('app_role_new') }}" class="btn btn-primary">
            <i class="fa fa-plus me-1"></i> Create New
        </a>
    </div>
    <div class="card shadow-sm">
        <div class="card-body p-0">
            <table class="table table-hover mb-0">
                <thead class="table-primary">
                    <tr>
                        <th>Id</th>
                        <th>Code</th>
                        <th>Label</th>
                        <th>Description</th>
                        <th class="text-center">Actions</th>
                    </tr>
                </thead>
                <tbody>
                {% for role in roles %}
                    <tr>
                        <td>{{ role.id }}</td>
                        <td>{{ role.code }}</td>
                        <td>{{ role.label }}</td>
                        <td>{{ role.description }}</td>
                        <td class="text-center">
                            <a href="{{ path('app_role_show', {'id': role.id}) }}" class="btn btn-outline-secondary btn-sm me-1">
                                <i class="fa fa-eye"></i>
                            </a>
                            <a href="{{ path('app_role_edit', {'id': role.id}) }}" class="btn btn-outline-primary btn-sm">
                                <i class="fa fa-edit"></i>
                            </a>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="5" class="text-center text-muted">No records found</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>
{% endblock %}
