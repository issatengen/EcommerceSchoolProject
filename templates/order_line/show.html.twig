{% extends 'base.html.twig' %}

{% block title %}OrderLine{% endblock %}

{% block body %}
    <h1>OrderLine</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ order_line.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ order_line.quantity }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ order_line.amount }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_order_line_index') }}">back to list</a>

    <a href="{{ path('app_order_line_edit', {'id': order_line.id}) }}">edit</a>

    {{ include('order_line/_delete_form.html.twig') }}
{% endblock %}
